# Backend-only environment. Copying this file is optional: setup:local creates
# a gitignored .env with a fresh random password for isolated local databases.
# Never put real connection strings into source, docs, screenshots or logs.
POSTGRES_PASSWORD=
DATABASE_URL=
MIGRATION_DATABASE_URL=
TEST_DATABASE_URL=
API_ADDR=127.0.0.1:8080
